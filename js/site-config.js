window.SITE_CONFIG = {
  /** Email для FormSubmit (https://formsubmit.co) — заявки приходят на эту почту */
  formSubmitEmail: 'donbasscargo@mail.ru',
  /**
   * ID счётчика Яндекс.Метрики (только цифры). Оставьте пустую строку, пока не создадите счётчик.
   * Скрипт подключается только после нажатия «Принять» в cookie-баннере.
   */
  yandexMetrikaId: ''
};
